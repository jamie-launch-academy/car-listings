<% if @manufacturer.errors.any? %>
  <%  @manufacturer.errors.full_messages.each do |msg| %>
    <li>  <%= msg %>  </li>
  <% end %>
<% end %>

<h2>Fill out the form below to add a manufacturer</h2>

<%= render "new_manufacturer_form" %>
<%= link_to "Back", manufacturers_path %>
